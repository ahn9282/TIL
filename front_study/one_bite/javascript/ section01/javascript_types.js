// 1. Number type
let num1 = 28;
let num2 = 1.5;
let num3 = - 2.5;


let inf = Infinity;
let mInf = -Infinity;
//수치 연산 실패 시 나오는 결과 - 연산 문제에서 더 안전하다.
let nan = NaN;
console.log(1 * "hello");

// 2. String Type
let myName = "ajs";
let str1 = '문자열1';
let myLocation = "마포";
let introduce = myName + myLocation;

let introduceText =`${myName} - ${introduce}`;

// 3. Boolean type
let isHuman = true;
let empty = true;
let hasNot = false;

// 4. Null Type - null
let emptyValue = null;

// 5. Undefined type - 값이 할당되지 않았을 경우 자동으로 할당되는 값
let none;
let undf = undefined;
